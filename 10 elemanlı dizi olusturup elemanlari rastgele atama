#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main(){
    int dizi[10];
    int i;
    srand(time(NULL));
    for(i=0;i<10;i++){
        dizi[i]=rand()%100;
    }
printf("dizinin elemanlari: ");
for(i=0;i<10;i++){
    printf("%d ",dizi[i]);
}
    return 0;
}
